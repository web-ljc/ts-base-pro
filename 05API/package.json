{
  "name": "05API",
  "version": "1.0.0",
  "description": "```js // 1. 项目配置文件 tsc --init // 2. 更改 tsconfig.json 文件中的 rootDir / outDir 地址 // 3. 执行命令，自定编译 tsc ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^2.9.13"
  },
  "dependencies": {
    "jquery": "^3.6.0"
  }
}
